{
  "name": "ar-solutions-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd har-xapp-website && npm run dev",
    "build": "cd har-xapp-website && npm run build",
    "start": "cd har-xapp-website && npm run start",
    "lint": "cd har-xapp-website && npm run lint"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "react-confetti": "^6.4.0",
    "react-use": "^17.6.0",
    "tailwind-merge": "^3.2.0"
  }
}
